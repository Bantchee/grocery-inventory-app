extends layout 

block content 
    h1=title 

    ul 
        each food in food_list 
            li 
                a(href=food.url) #{food.name} : #{food.description}
        else 
            li There are no farms