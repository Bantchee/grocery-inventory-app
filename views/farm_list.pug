extends layout 

block content 
    h1=title 

    ul 
        each farm in farm_list 
            li 
                a(href=farm.url) #{farm.name} : #{farm.address}
        else 
            li There are no farms